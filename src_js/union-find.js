/**
 * Initialise union-find data structure with N objects
 * (0 - N-1)
 */
function unionFind(n) {

}

/**
 * Add connection between two objects
 * @param {Number} p 
 * @param {Number} q 
 * @returns {undefined}
 */
function union(p, q) {

}

/**
 * Check for connection between two objects. Are they in the same
 * connected component?
 * @param {Number} p 
 * @param {Number} q 
 * @returns {Boolean}
 */
function connected(p, q) {

}

/**
 * Component identifier for p(0 to N-1)
 * @param {*} p 
 * @return {Number}
 */
function find(p) {

}

/**
 * Number of connected components
 * @returns {Number}
 */
function count() {

}